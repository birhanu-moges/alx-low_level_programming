#include "main.h"
/**
 * print_square - checkes digit vialue
 * Description: print square
 * @size: input to determine  square size
 * Return: (void)
 */

void print_square(int size)
{
	int i;
	int j;

	for (i = 0; i < size; i++)
	{
		for (j = 0; j < size; j++)
		{
			_putchar('#');
		}
		_putchar('\n');
	}

	if (n <= 0)
		_putchar('\n');
}
