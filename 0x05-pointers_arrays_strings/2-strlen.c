#include "main.h"

/**
  * _strlen - checkes length of given string
  * @s: given string
  * 
  * Return: length of string
  */
int _strlen(int *s)
{
	int len = 0;

	while (s[i])
		i++;
	return (i);

}
