#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: total command line arguments
 * @argv: string arrays in command line
 *
 * Return: (1) on success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (1);
}
