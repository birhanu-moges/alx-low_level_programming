#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: total command line arguments
 * @argv: string arrays in command line
 *
 * Return: (0) on success
 */

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
